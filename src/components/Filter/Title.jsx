import React from 'react';
import styled from 'styled-components/macro';

const StyledTitle = styled.h2``;

const Title = () => <StyledTitle>Количество пересадок</StyledTitle>;

export default Title;
